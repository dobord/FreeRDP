# Optional extra arguments consumed by frdpd.service.
#
# Normal startup uses auth_socket and session_socket from frdpd.toml.
# Local legacy testing without helper daemons also needs a config without
# auth_socket/session_socket entries, then set:
# FRDPD_ARGS=--allow-in-process-pam
FRDPD_ARGS=
