# 09. Implementation issues

Analysis date: 2026-06-16.

Build and syntax checks used for this pass:

```bash
cmake -S . -B /tmp/opencode/freerdp-current-build -DWITH_FRDPD=ON -DWITH_SERVER=ON -DWITH_SAMPLE=OFF
cmake --build /tmp/opencode/freerdp-current-build --target frdpd frdp-authd frdp-sesmand frdp-session-agent frdpd-ipc-demo frdpctl frdp-krb-authd -j2
cmake --build /tmp/opencode/freerdp-current-build --target frdpd frdp-authd frdpd-ipc-demo -j2
cmake -S . -B /tmp/opencode/freerdp-current-build -DWITH_SERVER=ON -DWITH_FRDPD=ON -DWITH_SAMPLE=OFF -DWITH_SHADOW=OFF -DWITH_PROXY=OFF
cmake --build /tmp/opencode/freerdp-current-build --target frdpd frdp-sesmand frdp-session-agent -j2
cmake -S . -B /tmp/opencode/freerdp-frdp-install-build -DWITH_FRDPD=ON -DWITH_SERVER=ON -DWITH_SAMPLE=OFF -DWITH_SHADOW=OFF -DWITH_PROXY=OFF
cmake --build /tmp/opencode/freerdp-frdp-install-build --target frdpd frdp-authd frdp-sesmand frdp-session-agent frdpctl frdp-krb-authd -j2
cmake --install /tmp/opencode/freerdp-frdp-install-build --component server --prefix /tmp/opencode/freerdp-install-frdp
cmake --build /tmp/opencode/freerdp-current-build --target frdpd frdp-sesmand frdp-session-agent -j2
cmake -S . -B /tmp/opencode/freerdp-frdp-package-build -DWITH_FRDPD=ON -DWITH_SERVER=ON -DWITH_SAMPLE=OFF -DWITH_SHADOW=OFF -DWITH_PROXY=OFF -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_INSTALL_SYSCONFDIR=/etc
DESTDIR=/tmp/opencode/freerdp-package-root cmake --install /tmp/opencode/freerdp-frdp-package-build --component server
rpmspec --define "_topdir /tmp/opencode/frdp-rpmbuild" -P /tmp/opencode/frdp-rpmbuild/SPECS/frdpd.spec
cc -fsyntax-only -Wall -Wextra server/frdp/frdp-authd/frdp-authd.c server/frdp/ipc/frdp-ipc.c
cc -fsyntax-only -Wall -Wextra server/frdp/frdp-sesmand/frdp-sesmand.c server/frdp/ipc/frdp-ipc.c
cc -fsyntax-only -Wall -Wextra server/frdp/frdp-session-agent/frdp-session-agent.c
cc -fsyntax-only -Wall -Wextra server/frdp/config/frdp-config.c server/frdp/frdpd/frdpd-ipc-demo.c server/frdp/ipc/frdp-ipc.c
# frdp-authd IPC negative-path smoke: frdpd-ipc-demo returns Authentication result: failure for invalid credentials.
# frdp-sesmand IPC startup smoke: --socket creates a 0600 Unix socket in a 0700 runtime directory.
```

The integrated `server/frdp/frdpd` target builds successfully and now includes the partial configuration
parser. The helper prototype sources in `server/frdp/frdp-authd`, `server/frdp/frdp-sesmand`,
`server/frdp/frdp-session-agent`, `server/frdp/frdp-krb-authd`, and `tools/frdpctl` now have CMake targets
under `WITH_FRDPD`, but they are not part of the canonical runtime topology.

Package build verification is still blocked in this environment: `rpmbuild -bb --nodeps` reaches `%build`
but the host RPM macro set does not provide `%cmake`, and `packaging/debian` currently contains only a draft
`control` file without `rules` or `changelog` for `dpkg-buildpackage`.

## Implemented since the previous issue pass

- The root-level `/frdpd` implementation is no longer present, so the earlier divergence/TLS-only/blocking-accept issues for that path are retired.
- `frdp-authd` now checks `mmap()` failure with `MAP_FAILED`; the remaining locked-memory concern is tracked in FRDP-016.
- `frdp-authd` no longer calls `initgroups()` in the broker process.
- `frdpd` now accepts `--config` and applies implemented `frdpd.toml` server/auth fields before CLI overrides; unsupported Kerberos/NTLM policy fields are rejected instead of being silently accepted.
- `frdp-config` now uses bounded string copies and strict integer/boolean parsing for fields it understands.
- `frdp-authd` IPC now rejects overlong/non-terminated auth fields, unsafe socket paths, and peers with a different effective UID on Linux; its socket is created with `0600` permissions.
- `frdp-sesmand` now includes `<grp.h>` and passes a strict syntax-only check.
- The standalone `frdp-sesmand` prototype now keeps the PAM handle for the session lifetime, runs `initgroups()` in the child after `fork()`, and records a process group for cleanup.
- The standalone `frdp-session-agent` now fails fast when `Xvfb` exec fails instead of entering an infinite sleep loop.
- `server/frdp` now builds and installs `frdp-authd`, `frdp-sesmand`, `frdp-session-agent`, and `frdpctl`; `frdpctl status` / `list-sessions` / `kill-session` can use `frdp-sesmand` session IPC, and `frdpctl reload` now sends a session-manager IPC request that makes `frdp-sesmand --config <path>` reread `[auth].pam_service` for future sessions. Full runtime reload for listener sockets, TLS material, channel policy, clipboard policy, and helper topology remains unimplemented. It also builds `frdpd-ipc-demo`, builds but does not install the `frdp-krb-authd` prototype when GSSAPI is available, and installs `frdpd.toml`.
- `server/frdp` now installs a dedicated non-interactive PAM service example at `/etc/pam.d/frdpd` through the CMake `server` component.
- `server/frdp` now installs `frdpd`, `frdp-authd`, and `frdp-sesmand` systemd service examples plus inactive SELinux/AppArmor draft policy examples through the CMake `server` component.
- Integrated `frdpd` now calls `pam_setcred(PAM_ESTABLISH_CRED)` after successful auth/account checks and `pam_setcred(PAM_DELETE_CRED)` during cleanup.
- Integrated `frdpd`, `frdp-authd`, and `frdp-sesmand` fail closed if core dumps/non-dumpable process hardening cannot be applied before handling credentials or sessions.
- `frdp-authd` no longer accepts passwords through positional command-line arguments; it only exposes the local `--socket` IPC server mode.
- `frdp-sesmand` no longer creates a hard-coded `nobody` session on startup; the standalone helper requires an explicit `--open-session <user>` request and `FRDP_SESMAND_ALLOW_STANDALONE=1` development opt-in.
- `frdpd` now requires `frdp-authd` and `frdp-sesmand` sockets for normal startup; the old direct PAM path is available only in builds configured with `-DWITH_FRDPD_IN_PROCESS_PAM=ON`.
- `frdpd` can now route password-backed auth/account checks through `frdp-authd` over IPC when `auth_socket` or `--auth-socket=<absolute-path>` is configured.
- `frdp-authd` IPC requests now carry the remote host in addition to the normalized user and password; PAM service selection remains broker-side and is configured with `frdp-authd --pam-service`.
- Integrated `frdpd` now generates a per-peer correlation id, includes it on accept/auth/logon/activate/disconnect logs, and sends it in `frdp-authd` IPC auth requests; `frdp-authd` uses a valid incoming id for syslog audit events and falls back to a local UUID when the request omits or sends an invalid id.
- `frdp-sesmand` now exposes a guarded local `--socket` IPC server; integrated `frdpd` can open and close managed sessions through configured `session_socket` / `--session-socket=<absolute-path>` after authentication and static-channel policy checks pass.
- Shared FRDP IPC clients now set send/receive timeouts and use `MSG_NOSIGNAL` when available so disconnected IPC peers return errors instead of terminating the process with `SIGPIPE`.
- Session IPC now passes session ids and correlation ids into `frdp-session-agent`, which includes them in startup and display-backend exit logs.
- `frdp-session-agent` now reports readiness only after Xvfb `exec()`, X display open, and XTest checks pass, and `frdp-sesmand` fails session creation if agent readiness is not reported.
- Session IPC now creates a root-owned per-session agent control socket, returns the path to `frdpd`, forwards keyboard/mouse/Unicode/synchronize input events to `frdp-session-agent`, and has the agent validate root peer credentials plus session ids before injecting scancode keyboard, Unicode BMP text, and mouse events into Xvfb through XTest/X11.
- `frdpd` now pulls bounded raw framebuffer tiles from the session agent over the same root-only control socket, the agent uses XDamage-backed dirty-tile state to return selected dirty tiles or clean-tile responses when available, `frdpd` suppresses unchanged tiles with per-peer hashes, and changed tiles are sent to the RDP client as raw FreeRDP bitmap updates or negotiated minimum-color-loss/no-subsampling NSCodec `SurfaceBits` when the encoded tile is smaller.
- `frdp-sesmand` session IPC now performs POSIX account lookup, child `initgroups()`/uid/gid drop, PAM session ownership, and agent process-group cleanup on close requests.
- `frdpd` now enables RDP dynamic resolution/display-control settings, handles monitor-layout changes, asks the session agent to resize the Xvfb display through XRandR, and updates peer geometry only after the agent confirms the backend resize.
- `frdpd` now uses a bounded raw-tile output scheduler with a shorter active-peer wait interval, a two-tile pump cap, a pump time check, and a reduced frame IPC timeout to improve initial/full-refresh and dirty backlog progress while keeping the slow-agent timeout budget comparable to the prior single-tile path.
- `frdpd` now enforces configured `max_connections` before authentication, channel checks, or managed-session creation; it can be set from `frdpd.toml` or overridden with `--max-connections=<n>`.
- `frdpd` now applies configurable static virtual-channel filtering during client capability processing; `[channels].static_mode` supports blocklist/allowlist semantics, defaults to empty blocklist mode, uses exact `static_deny` / `static_allow` lists, validates raw `CHANNEL_DEF` names fail-closed, and keeps `drdynvc` guard-denied until DVC transport and handlers are explicitly enabled. `[channels].dynamic_mode` plus `dynamic_deny` / `dynamic_allow` is parsed as exact-name policy for the DVC authorization callback. `[clipboard]` now parses disabled-by-default text clipboard policy with explicit direction and bounded maximum text payload before runtime handling is enabled. The server-side WTS API now has a DVC authorization hook before dynamic-channel `CREATE_REQUEST` emission, and `frdpd` opens/checks/closes a WTS virtual-channel manager without auto-opening `drdynvc`, with that callback installed. Focused CTest coverage exercises config parsing, clipboard policy validation, `CHANNEL_DEF` validation, and the WTS runtime DVC authorization deny path. Useful channel handlers are still not implemented.
- `frdpd`, `frdp-authd`, `frdp-sesmand`, `frdp-session-agent`, and `frdpctl` now escape client/IPC/CLI/environment-supplied output fields before writing peer, auth, channel, session, helper lifecycle, or admin CLI events; password payloads remain unlogged.
- `frdpctl` now escapes IPC-supplied session ids, users, display names, session-manager errors, and session-manager socket paths before writing them to the admin terminal.
- Shared FRDP IPC client sockets and `frdp-authd` / `frdp-sesmand` listener plus accepted IPC client sockets are now created close-on-exec where supported, with `fcntl(FD_CLOEXEC)` fallback; the `frdp-sesmand` per-session agent control socket remains intentionally inheritable for `frdp-session-agent` startup.
- `frdp-sesmand` display allocation now skips display numbers already present in its session registry or visible through local X lock/socket paths, but allocation is still in-memory and not reconnect-aware.
- `frdp-sesmand` now handles SIGINT/SIGTERM by stopping the IPC accept loop and cleaning up tracked agent process groups, PAM sessions, and runtime sockets before exit.
- `frdpd`, `frdp-authd`, `frdp-sesmand`, and `frdp-session-agent` install explicit non-restarting SIGINT/SIGTERM handlers so stop requests are observed by their main loops and fail closed if handler installation fails; `frdp-authd` also closes and unlinks its IPC socket on graceful stop, and a running `frdp-session-agent` terminates its display backend on stop.
- Focused CTest coverage now starts live `frdp-authd` and `frdp-sesmand` helpers together, runs default-build `frdpd` with both live helper sockets, verifies startup reaches the certificate/key gate instead of the helper-topology gate, and then proves both helper service loops remain responsive.

## Remaining issues

| ID | Issue | Severity | Confidence | Evidence | Impact | Recommended action |
|---|---|---:|---:|---|---|---|
| FRDP-001 | Helper binaries are built, normal `frdpd` startup requires both auth/session IPC paths, and the live helper topology has startup smoke coverage, but authenticated end-to-end login coverage is still incomplete. | 55% | 100% | `server/frdp/CMakeLists.txt` builds helper targets and install rules under `WITH_FRDPD`; `server/frdp/frdpd/frdpd_auth.c` calls `frdp-authd` over IPC when `auth_socket` is configured; `server/frdp/frdpd/frdpd.c` calls `frdp-sesmand` over IPC when `session_socket` is configured. `server/frdp/frdpd/frdpd.c` rejects normal startup unless both helper sockets are configured, and the shipped `frdpd.toml` points at the installed helper unit sockets. `server/frdp/test/TestFreeRDPFrdpServiceIpc.c` starts live `frdp-authd` and `frdp-sesmand`, runs `frdpd` with both live sockets through the helper-topology gate, and checks both helper service loops remain responsive afterward. | Built helper binaries are now covered at the startup-topology boundary, but they can still drift from production behavior until successful and denied login/session flows are exercised through a real RDP client and the development fallback is deleted. | Add authenticated end-to-end tests for the canonical helper topology, then remove the peer-worker PAM session fallback. |
| FRDP-002 | IPC now connects `frdpd` to auth, session, agent input injection, and a minimal raw framebuffer/resize path, but Kerberos helper IPC and production framebuffer handling are not implemented. | 39% | 95% | `server/frdp/frdpd/frdpd_auth.c` sends `FRDP_IPC_AUTH_REQUEST_V2` to `frdp-authd`; `server/frdp/frdpd/frdpd.c` sends `FRDP_IPC_SESSION_REQUEST` and `FRDP_IPC_SESSION_CLOSE_REQUEST` to `frdp-sesmand`; managed session responses include an agent control socket and `frdpd` forwards input events, framebuffer tile requests, and resize requests to it. `frdp-session-agent` injects scancode keyboard, Unicode BMP text, and mouse events, serves raw Xvfb framebuffer tiles with XDamage-backed dirty/clean-tile responses when available, and applies resize requests through XRandR. `frdpd` now has bounded raw-tile output scheduling and opportunistic minimum-color-loss/no-subsampling NSCodec `SurfaceBits` for smaller negotiated tiles, but no code path invokes `frdp-krb-authd`, and RFX/RDPGFX production codec policy is not implemented. | The intended multi-process auth/session lifecycle is partially operational, but Kerberos-first auth and a production-quality desktop remain unavailable. | Add production codec selection/encoder scheduling, real-client resize/churn interoperability tests, and Kerberos helper IPC in later slices. |
| FRDP-003 | The integrated `frdpd` normal runtime no longer exposes the legacy in-process PAM fallback, but the code is still available behind an explicit development build option. | 88% | 95% | Normal startup requires both `auth_socket` and `session_socket`; without helper sockets, default builds reject startup in `server/frdp/frdpd/frdpd.c`. `--allow-in-process-pam` and `--no-pam-session` are accepted only when the target is built with `-DWITH_FRDPD_IN_PROCESS_PAM=ON`, and the direct fallback body in `server/frdp/frdpd/frdpd_auth.c` is compiled only in that mode. | The intended privilege boundary is now the default runtime path, but the development-only fallback still exists in-tree and can retain long-lived PAM state if explicitly enabled. | Keep the development option disabled in packaging/CI, then delete the fallback once helper-topology test coverage is sufficient. |
| FRDP-005 | Desktop data path remains incomplete: backend input injection, raw framebuffer tiles, prototype resize, basic raw output scheduling, opportunistic NSCodec compression, static channel negotiation filtering, and DVC authorization plumbing exist, but production graphics handling and useful channels are missing. | 53% | 100% | `server/frdp/frdpd/frdpd.c` forwards keyboard/mouse/Unicode/synchronize events to the per-session agent control socket, periodically pulls bounded raw framebuffer tiles for FreeRDP bitmap updates, handles monitor-layout changes, forwards resize requests when `session_socket` is enabled, limits raw tile output with a pump interval/tile/time budget, sends negotiated minimum-color-loss/no-subsampling NSCodec `SET_SURFACE_BITS` only when the encoded tile is smaller than the raw tile, filters client-requested static virtual channels according to `[channels].static_mode` plus exact `static_deny` / `static_allow` lists, and installs a DVC authorization callback while leaving `drdynvc` auto-open disabled. `server/frdp/frdp-session-agent/frdp-session-agent.c` injects scancode keyboard, Unicode BMP text, and mouse events into Xvfb through XTest/X11, captures requested Xvfb tiles with `XGetImage`, uses XDamage-backed dirty-tile state to return selected dirty tiles or clean-tile responses when the extension is available, and applies resize requests through XRandR. `frdpd` suppresses unchanged tiles with per-peer hashes and invalidates that cache for refresh/suppress-output/resize. IME/layout-safe text input, supplementary-plane Unicode, `drdynvc` transport enablement, useful channel handlers, and RFX/RDPGFX/full codec policy are not implemented. | A client can receive initial visible output in principle, but performance and completeness are not sufficient for daily interactive desktop use. | Implement production codec selection/encoder scheduling, IME/layout-safe text input, useful channel handlers, and runtime interoperability tests. |
| FRDP-006 | Static channel capability filtering, DVC authorization plumbing, preparatory dynamic filtering, and text clipboard policy parsing are implemented, but `cliprdr`/`drdynvc` transport enablement and useful virtual channels are not implemented. | 60% | 95% | `server/frdp/frdpd/frdpd.c` registers `ClientCapabilities` and filters requested static virtual channels before opening a managed session. It also opens/checks/closes a WTS virtual-channel manager per peer without auto-opening `drdynvc`, and installs a DVC authorization callback that applies `[channels]` dynamic policy to server-created DVC names. `server/frdp/frdpd/channel_policy.c` validates raw `CHANNEL_DEF` entries fail-closed, exposes exact-match static and dynamic policy helpers, and keeps `drdynvc` guard-denied. `server/frdp/config/frdp-config.c` parses `[channels].static_mode` / `[channels].dynamic_mode` as `blocklist`/`blacklist` or `allowlist`/`whitelist`, defaults both to empty blocklist mode, rejects inactive list keys even when empty, rejects `drdynvc` in allow-lists, parses exact comma-separated channel lists, and parses `[clipboard].mode`, `direction`, and `max_text_bytes` with disabled defaults, explicit non-disabled direction for text mode, and a `1..1048576` payload limit. `include/freerdp/channels/wtsvc.h` and `libfreerdp/core/server.c` expose `WTSVirtualChannelManagerSetDVCChannelAuthorizationCallback`, which can deny a server-created DVC before `WTSVirtualChannelOpenEx` sends a `CREATE_REQUEST`; `libfreerdp/core/test/TestWtsDvcAuthorization.c` covers that WTS runtime deny path and verifies the callback is not reached before `drdynvc` is joined/ready. `server/frdp/test/TestFreeRDPFrdpConfig.c` covers default empty blocklist, explicit static/dynamic allow-lists and deny-lists, clipboard policy validation, malformed config rejection, guarded `drdynvc`, inactive-list rejection, and `CHANNEL_DEF` validation. No `cliprdr`, `rdpsnd`, `audin`, `rdpdr`, or useful DVC handler is wired in `frdpd`; `drdynvc` still cannot pass the static-channel guard, and `frdp-session-agent` still has a TODO for clipboard/audio channels. | Static channel filtering, dynamic policy, and text clipboard bounds are configurable with focused coverage, but MVP clipboard/audio requirements are not met and dynamic virtual channels remain effectively disabled by the `drdynvc` guard and missing handlers. | Wire text-only `cliprdr` through the explicit clipboard policy and per-session agent, then enable `drdynvc` only for explicit useful handlers and add client-interoperability DVC policy tests. |
| FRDP-007 | Client interoperability and AD/SSSD lab validation are not represented by automated tests or checked artifacts. | 68% | 90% | The client matrix and AD/SSSD lab deliverables remain unchecked. `server/frdp/test/TestFreeRDPFrdpConfig.c` covers static/dynamic channel config parsing, fail-closed policy decisions, static `CHANNEL_DEF` validation, filter modes, and `max_connections` parsing. `server/frdp/test/TestFreeRDPFrdpCtlIpc.c` covers local `frdpctl` status/list/kill/reload session IPC behavior against a fake secure Unix socket, and `TestFreeRDPFrdpCtl.cmake` covers CLI fail-closed parsing including malformed `reload` arguments, but there are still no end-to-end `frdpd` login/session/client interoperability tests. | Build and unit-test success can hide NLA/client/PAM/SSSD interoperability failures. | Add lab scripts and smoke tests for accepted login, denied login, client connection attempts, and domain user normalization. |
| FRDP-008 | `server/frdp/frdpd` consumes only a subset of `frdpd.toml`. | 42% | 100% | `server/frdp/frdpd/frdpd.c` applies `listen`, implemented `security=nla`, `tls_cert`, `tls_key`, `max_connections`, `mode=pam-sssd`, `pam_service`, absolute `auth_socket`, absolute `[session].session_socket`, static channel filter mode/lists, and dynamic channel filter mode/lists through the installed DVC authorization callback. It also accepts `--max-connections=<n>` as a CLI override. `server/frdp/config/frdp-config.c` parses `[clipboard]` text policy bounds but does not yet wire a runtime clipboard handler, and rejects unsupported Kerberos/NTLM, unknown session fields, unknown channel fields, unknown clipboard fields, and `audit` fields. | Kerberos policy, clipboard runtime, `drdynvc` transport enablement/useful handlers, and audit config remain unavailable until enforcement exists, but they no longer appear as silently accepted active policy. | Extend config parsing and application incrementally, failing closed on invalid values and documenting unsupported fields until they are wired. |
| FRDP-009 | Packaging remains draft-only and is not package-build verified. | 40% | 100% | `server/frdp/CMakeLists.txt` now installs the listed runtime helper binaries, `frdpd.toml`, the `frdpd` PAM service example, systemd service examples, and inactive SELinux/AppArmor draft policy examples; package-style CMake install was verified under `DESTDIR`, but no RPM/DEB package build has completed. Local RPM build is blocked by missing host `%cmake` RPM macros, Debian packaging lacks `rules`/`changelog`, and MAC profiles are not validated or activated. | Packages can still fail in CI, and installed draft policy examples are not sufficient production confinement. | Add RPM/DEB package build verification, distro-specific service scriptlet validation, and validated SELinux/AppArmor policy before treating packaging as complete. |
| FRDP-011 | Audit correlation is partial, not end-to-end. | 38% | 95% | `server/frdp/frdpd/frdpd.c` generates a per-peer id and logs accept/auth/logon/activate/disconnect plus static channel policy denials with it; client-supplied hostnames, authenticated usernames, static channel names, and IPC-supplied session ids, display names, agent socket paths, and session-manager error strings are escaped before peer/channel/session logging. `server/frdp/frdpd/frdpd_auth.c` sends the id in `FRDP_IPC_AUTH_REQUEST_V2`; `server/frdp/frdpd/frdpd.c` sends it in session open/close IPC; `server/frdp/frdp-authd/frdp-authd.c`, `server/frdp/frdp-sesmand/frdp-sesmand.c`, and `server/frdp/frdp-session-agent/frdp-session-agent.c` use ids in auth/session/agent audit events. Structured audit config and useful channel handler decisions are not implemented yet. | Current auth/session/agent startup logs and static channel denials can be joined on helper IPC paths, but future channel data decisions still cannot be correlated end-to-end. | Carry the same id through structured audit configuration and useful channel handler policy logs before marking audit correlation complete. |
| FRDP-012 | `frdp-sesmand` has a session service endpoint, but reconnect and production lifecycle management are incomplete. | 60% | 100% | `server/frdp/frdp-sesmand/frdp-sesmand.c` exposes `--socket` for `FRDP_IPC_SESSION_REQUEST` and `FRDP_IPC_SESSION_CLOSE_REQUEST`; `server/frdp/frdpd/frdpd.c` can call it after authentication plus static-channel policy checks and schedules bounded close retries if disconnect cleanup IPC fails. `frdp-sesmand` handles SIGINT/SIGTERM by stopping the IPC accept loop and cleaning tracked sessions before exit, and a running `frdp-session-agent` observes SIGINT/SIGTERM so its display backend is terminated during stop. Reconnect lookup, persistent session metadata, durable cleanup after prolonged manager outage, logind/cgroups, and agent protocol supervision remain missing. | Sessions can be opened and closed by authenticated peers on the helper path, and manager shutdown cleans tracked sessions, but production reconnect/resource lifecycle requirements are not met. | Add reconnect semantics, persistent/session-scoped display allocation, durable cleanup/reconciliation, logind/cgroup integration, and agent protocol supervision. |
| FRDP-013 | Display allocation remains prototype-level, and resize behavior has only focused agent-side smoke coverage. | 56% | 100% | `server/frdp/frdp-sesmand/frdp-sesmand.c` keeps `next_display` in memory and now skips display numbers already present in its session registry or visible through `/tmp/.X<N>-lock` / `/tmp/.X11-unix/X<N>`; `frdpd` can pass negotiated initial geometry and runtime monitor-layout resize requests through session/agent IPC, `frdp-session-agent` applies resize requests through XRandR, and `TestFreeRDPFrdpAgentStop` validates the inherited control-socket resize request/response path against an Xvfb-backed agent when run as root. | Display allocation is still not persistent or atomic across manager restarts/races, and resize may still fail for real clients, multimonitor layouts, churn, or backend combinations that are not covered by the focused Xvfb smoke test. | Reserve display numbers atomically, persist/session-scope allocation state, add reconnect-aware display metadata, and add real-client resize interoperability tests. |
| FRDP-014 | `frdp-session-agent` launches Xvfb, injects validated keyboard/mouse/text input, serves raw framebuffer tiles, and applies resize requests, but lacks production framebuffer/channel features. | 45% | 100% | `server/frdp/frdp-session-agent/frdp-session-agent.c` verifies Xvfb `exec()` before logging backend startup, opens the X display, requires XTest, accepts root-only input/frame/resize requests for the current session id/correlation id, injects scancode keyboard, Unicode BMP text via temporary unused X keycode mapping, and mouse events, captures requested Xvfb tiles with `XGetImage`, tracks XDamage dirty tiles for selected dirty-tile or clean-tile responses, applies display resize requests through XRandR, and handles steady-state SIGINT/SIGTERM by terminating Xvfb without later Xlib calls that could fatal-exit after process-group stop; `TestFreeRDPFrdpAgentStop` starts an Xvfb-backed agent with an inherited control socket, verifies that a non-root control peer is rejected, and verifies a root-peer resize request/response at the current Xvfb geometry when the test is run through `sudo`; `frdpd` suppresses unchanged tiles before sending bitmap updates or opportunistic NSCodec `SurfaceBits`. RFX/RDPGFX production codec policy, IME/layout-safe text input, clipboard, and audio remain TODOs. | The session backend can fail closed on missing Xvfb/XTest and can provide basic input plus raw visual output/resize, but performance and channel support remain prototype-level. | Add production encoder scheduling/codec policy, IME/layout-safe text input, and initial clipboard/audio features. |
| FRDP-015 | `frdp-krb-authd` is a build-only skeleton, not a working Kerberos/CredSSP acceptor. | 85% | 100% | `server/frdp/frdp-krb-authd/frdp-krb-authd.c:36-56` accepts a token argument but does not decode it before `gss_accept_sec_context()`; `server/frdp/frdp-krb-authd/frdp-krb-authd.c:39-40` hard-codes the keytab path; mapping uses raw `getpwnam()` on the displayed principal; `server/frdp/CMakeLists.txt` intentionally does not install it. | Kerberos-first authentication cannot be validated, and principal-to-account mapping is likely wrong for real AD principals. | Implement SPNEGO token ingestion from CredSSP, configured keytab/SPN handling, SSSD-backed principal mapping, and PAM account/session integration before installing/package-enabling it. |
| FRDP-016 | Integrated secret hardening is partial. | 70% | 90% | `server/frdp/frdpd`, `server/frdp/frdp-authd`, and `server/frdp/frdp-sesmand` fail closed if core dumps/non-dumpable process hardening cannot be applied. `server/frdp/frdp-authd` locks and wipes temporary PAM password buffers, and `server/frdp/frdpd/frdpd_auth.c` locks-or-fails-closed and wipes temporary CredSSP password copies plus the auth-IPC request password field before releasing them. The auth broker path is required for normal startup, and the explicit in-process PAM fallback is compiled only with `-DWITH_FRDPD_IN_PROCESS_PAM=ON`. | Password material can remain exposed in daemon memory longer than intended through FreeRDP identity structures and through explicitly enabled development fallback builds. | Keep scoped locked buffers for any remaining temporary copies and remove the development-only peer-worker PAM fallback when it is no longer needed. |

## Build observations

- The integrated `frdpd` target and helper targets build successfully in `/tmp/opencode/freerdp-current-build`.
- The isolated `server` component install check succeeds for FRDP binaries/config when unrelated shadow/proxy server targets are disabled.
- The focused `freerdp`, `freerdp-server`, `frdpd`, `frdp-sesmand`, and `frdp-session-agent` targets build successfully after the XTest input, raw framebuffer tile, unchanged-tile suppression, XDamage clean-tile, dirty-tile scheduling, XRandR display-resize, bounded raw output scheduling, opportunistic NSCodec `SurfaceBits`, Unicode BMP text input, static channel filtering, static/dynamic channel validation, guarded `drdynvc`, DVC authorization hook and runtime deny-path coverage, `frdpd` VCM wiring, `max_connections`, log-field escaping, and scoped `frdpd` password-copy locking changes.
- The `TestFreeRDPFrdpConfig` CTest covers static/dynamic channel config parsing, default empty blocklist behavior, exact allow-list and deny-list matching, malformed channel config rejection, guarded `drdynvc`, `CHANNEL_DEF` validation, and `max_connections` parsing.
- The `TestFreeRDPFrdpIpc`, `TestFreeRDPFrdpCtl`, `TestFreeRDPFrdpCtlIpc`, `TestFreeRDPFrdpHelperStop`, `TestFreeRDPFrdpServiceIpc`, and `TestFreeRDPFrdpAgentStop` CTests cover secure IPC socket path validation, exact-read short-read failure, malformed CLI arguments including `frdpctl reload`, local session IPC request/response behavior for `status`, `list-sessions`, `kill-session`, and the no-op `reload` acknowledgement against a test-owned secure Unix socket, helper SIGTERM socket cleanup, live auth/session helper topology startup through `frdpd`, session-agent SIGTERM cleanup against Xvfb when available, session-agent control peer rejection for non-root callers, root-peer resize request/response handling at the current Xvfb geometry, and escaping of IPC-supplied fields before CLI output.
- The build emits existing FreeRDP deprecation warnings unrelated to the new PAM/SSSD/NLA prototype.
- No package build, PAM login, AD/SSSD lab, agent input/framebuffer runtime smoke beyond the focused resize control-IPC path, or RDP client interoperability test was run in this pass.
