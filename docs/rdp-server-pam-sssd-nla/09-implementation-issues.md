# 09. Implementation issues

Analysis date: 2026-06-16.

Build and syntax checks used for this pass:

```bash
cmake -S . -B /tmp/opencode/freerdp-current-build -DWITH_FRDPD=ON -DWITH_SERVER=ON -DWITH_SAMPLE=OFF
cmake --build /tmp/opencode/freerdp-current-build --target frdpd frdp-authd frdp-sesmand frdp-session-agent frdpd-ipc-demo frdpctl frdp-krb-authd -j2
cmake --build /tmp/opencode/freerdp-current-build --target frdpd frdp-authd frdpd-ipc-demo -j2
cmake -S . -B /tmp/opencode/freerdp-current-build -DWITH_SERVER=ON -DWITH_FRDPD=ON -DWITH_SAMPLE=OFF -DWITH_SHADOW=OFF -DWITH_PROXY=OFF
cmake --build /tmp/opencode/freerdp-current-build --target frdpd frdp-sesmand frdp-session-agent -j2
cmake -S . -B /tmp/opencode/freerdp-frdp-install-build -DWITH_FRDPD=ON -DWITH_SERVER=ON -DWITH_SAMPLE=OFF -DWITH_SHADOW=OFF -DWITH_PROXY=OFF
cmake --build /tmp/opencode/freerdp-frdp-install-build --target frdpd frdp-authd frdp-sesmand frdp-session-agent frdpctl frdp-krb-authd -j2
cmake --install /tmp/opencode/freerdp-frdp-install-build --component server --prefix /tmp/opencode/freerdp-install-frdp
cmake --build /tmp/opencode/freerdp-current-build --target frdpd frdp-sesmand frdp-session-agent -j2
cmake -S . -B /tmp/opencode/freerdp-frdp-package-build -DWITH_FRDPD=ON -DWITH_SERVER=ON -DWITH_SAMPLE=OFF -DWITH_SHADOW=OFF -DWITH_PROXY=OFF -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_INSTALL_SYSCONFDIR=/etc
DESTDIR=/tmp/opencode/freerdp-package-root cmake --install /tmp/opencode/freerdp-frdp-package-build --component server
rpmspec --define "_topdir /tmp/opencode/frdp-rpmbuild" -P /tmp/opencode/frdp-rpmbuild/SPECS/frdpd.spec
cc -fsyntax-only -Wall -Wextra server/frdp/frdp-authd/frdp-authd.c server/frdp/ipc/frdp-ipc.c
cc -fsyntax-only -Wall -Wextra server/frdp/frdp-sesmand/frdp-sesmand.c server/frdp/ipc/frdp-ipc.c
cc -fsyntax-only -Wall -Wextra server/frdp/frdp-session-agent/frdp-session-agent.c
cc -fsyntax-only -Wall -Wextra server/frdp/config/frdp-config.c server/frdp/frdpd/frdpd-ipc-demo.c server/frdp/ipc/frdp-ipc.c
# frdp-authd IPC negative-path smoke: frdpd-ipc-demo returns Authentication result: failure for invalid credentials.
# frdp-sesmand IPC startup smoke: --socket creates a 0600 Unix socket in a 0700 runtime directory.
```

The integrated `server/frdp/frdpd` target builds successfully and now includes the partial configuration
parser. The helper prototype sources in `server/frdp/frdp-authd`, `server/frdp/frdp-sesmand`,
`server/frdp/frdp-session-agent`, `server/frdp/frdp-krb-authd`, and `tools/frdpctl` now have CMake targets
under `WITH_FRDPD`, but they are not part of the canonical runtime topology.

Package build verification is still partial, but both preview package formats now have local smoke evidence:
the root `debian/` metadata supports a verified server-only `dpkg-buildpackage` binary build whose
default-on NTLM package includes a working `winpr-hash --password-stdin`, and
`packaging/rpm/frdpd.spec` supports a verified server-only `rpmbuild -bb --nodeps` binary build when
Fedora-style CMake/systemd macros are supplied by the distro or by a local Ubuntu smoke overlay. Package
signing and reproducible-build notes now define the release build contract, while dependency-checked RPM
distro CI and full distro policy validation remain open.

## Implemented since the previous issue pass

- The root-level `/frdpd` implementation is no longer present, so the earlier divergence/TLS-only/blocking-accept issues for that path are retired.
- `frdp-authd` now checks `mmap()` failure with `MAP_FAILED`; the remaining locked-memory concern is tracked in FRDP-016.
- `frdp-authd` no longer calls `initgroups()` in the broker process.
- `frdpd` now accepts `--config` and applies implemented `frdpd.toml` server/auth fields before CLI overrides, including enforced `ntlm_fallback = false`; unsupported Kerberos identity policy fields are rejected instead of being silently accepted.
- `frdp-config` now uses bounded string copies and strict integer/boolean parsing for fields it understands.
- `frdp-authd` IPC now rejects overlong/non-terminated auth fields, unsafe socket paths, and peers with a different effective UID on Linux; its socket is created with `0600` permissions.
- `frdp-sesmand` now includes `<grp.h>` and passes a strict syntax-only check.
- The standalone `frdp-sesmand` prototype now keeps the PAM handle for the session lifetime, applies a bounded supplementary group list in the child after `fork()`, and records a process group for cleanup.
- The standalone `frdp-session-agent` now fails fast when `Xvfb` exec fails instead of entering an infinite sleep loop.
- `server/frdp` now builds and installs `frdp-authd`, `frdp-sesmand`, `frdp-session-agent`, and `frdpctl`; `frdp-authd --config <path>` and `frdp-sesmand --config <path>` both read implemented policy from `frdpd.toml`, installed helper units use that shared config, `frdpctl status` / `list-sessions` / `kill-session` can use `frdp-sesmand` session IPC, and `frdpctl reload` now sends a session-manager IPC request that makes `frdp-sesmand` reread `[auth].pam_service` plus supported `[session]` resource guards for future sessions with an explicit applied-policy response. Separately, `frdpd --config <path>` handles `SIGHUP` by parsing the complete file and atomically publishing only `[channels]` and `[clipboard]` to future peers; active peers retain connection-time snapshots and a parse failure retains the last valid policy. Listener, TLS, authentication/NTLM, helper-topology, session, and audit changes still require daemon restart. It also builds `frdpd-ipc-demo`, builds but does not install the `frdp-krb-authd` prototype when GSSAPI is available, and installs `frdpd.toml`.
- `server/frdp` now installs a dedicated non-interactive PAM service example at `/etc/pam.d/frdpd` through the CMake `server` component.
- `server/frdp` now installs `frdpd`, `frdp-authd`, and `frdp-sesmand` systemd service examples with baseline sandboxing directives, including clock and hostname protection, plus inactive SELinux/AppArmor draft policy examples through the CMake `server` component. The server component also installs manuals for all six packaged commands, and each daemon unit links to its manual through `Documentation=man:`; `TestFreeRDPFrdpSystemd` verifies those resolvable links and the expected hardening directives when `systemd-analyze` is available, the SELinux draft is compiled/packaged by CTest when `checkmodule` and `semodule_package` are available, and the AppArmor draft is parser-validated by CTest when `apparmor_parser` is available.
- Integrated `frdpd` now calls `pam_setcred(PAM_ESTABLISH_CRED)` after successful auth/account checks and `pam_setcred(PAM_DELETE_CRED)` during cleanup.
- Integrated `frdpd`, `frdp-authd`, and `frdp-sesmand` fail closed if core dumps/non-dumpable process hardening cannot be applied before handling credentials or sessions.
- `frdp-authd` no longer accepts passwords through positional command-line arguments; it only exposes the local `--socket` IPC server mode.
- `frdp-sesmand` no longer creates a hard-coded `nobody` session on startup; the standalone helper requires an explicit `--open-session <user>` request and `FRDP_SESMAND_ALLOW_STANDALONE=1` development opt-in.
- `frdpd` now requires configured `frdp-authd` and `frdp-sesmand` sockets for normal startup and performs bounded role-bound health checks before listening; the old direct peer-worker PAM fallback build option and CLI path have been removed.
- `frdpd` can now route password-backed auth/account checks through `frdp-authd` over IPC when `auth_socket` or `--auth-socket=<absolute-path>` is configured.
- `frdp-authd` IPC requests now carry the remote host in addition to the normalized user and password; PAM service selection remains broker-side, `frdp-authd --config <path>` reads `[auth].pam_service`, and `--pam-service` remains the direct standalone override.
- Auth-request IPC decoding clears the caller-owned request before reading, including on truncated payloads, and `frdp-authd` wipes the full decoded request after every handling attempt so a failed or completed broker transaction does not retain the password-bearing structure on its stack.
- `frdp-authd` applies independent process-local failure budgets to the normalized account and remote source before PAM: each fixed-capacity table permits 10 credential/account denials per 120-second window, fails closed when it cannot track another key, uses the NSS/SSSD canonical account name when available and an ASCII case-folded fallback for unresolved names, and binds aliases to a shared canonical budget when a denied PAM transaction supplies a valid `PAM_USER`. Bindings are bounded, expire with the policy window, conservatively merge existing counters, reject a conflicting alias-to-canonical rebinding within the active window, retain source failures across successful logins, and clear the successful account's shared failures. PAM/NSS infrastructure failures fail closed without poisoning denial budgets. A previously unseen alias can still incur one PAM attempt, broker restart resets the state, and distributed enforcement remains open. Focused policy tests cover thresholds, canonical/fallback key behavior, alias merge/chain compression/conflicting-rebind rejection/expiry/capacity, clock regression and clearing; a deterministic deny-only live-helper component test independently proves account and source threshold responses without depending on host PAM policy or backoff.
- When `libpam-wrapper` is available, focused CTest coverage starts the real `frdp-authd` against isolated PAM service stacks. It proves post-account `PAM_USER` canonicalization into the NSS identity and signed authorization response, clean account denial, a shared denial budget across two PAM-discovered aliases with the next known-alias request rejected before PAM, and a distinct fail-closed service-error response for unavailable PAM identity infrastructure; only credential/account denials consume the broker failure budgets.
- The same provider-backed component gate starts the real `frdp-sesmand` with a test-only PAM module that records account, credential establish/delete, and session-open phases before deterministically returning `PAM_SESSION_ERR`. The signed V3 request fails cleanly, the registry remains empty, and the runtime directory contains no agent socket, display reservation, or durable metadata.
- Auth-response and V3 session-request decoders likewise clear caller-owned authorization-token outputs before header/payload processing, while auth-token creation and verification clear failed outputs and wipe local nonce, payload, HMAC, digest, and key buffers on every exit path.
- Default-on `WITH_FRDPD_NTLM` now provides build-time removable NTLM proof verification through a protected WinPR SAM. Runtime enablement requires an explicit absolute `ntlm_sam_file`; the daemon rejects missing, non-regular, multiply linked, non-owner-only, wrongly owned, empty, comments-only, or structurally malformed files before helper probing or listening. Before PAM, the proof identity is bound to the configured PAM-normalized delegated identity, accepting ASCII-case-insensitive split, down-level, or UPN syntax (with exact non-ASCII bytes) while rejecting empty components, embedded NUL/padding data, separators in explicit domains, mixed/repeated separators, unterminated fields, or differently named retained domains without inference. The authenticated proof may add a domain omitted by delegated FreeRDP password credentials; if the delegated/PAM identity retains a domain, the proof must retain and match it. CredSSP password credentials then pass through the mandatory `frdp-authd -> PAM/SSSD` gate. `winpr-hash --password-stdin` provides bounded binary SAM output generation without exposing an approved plaintext provisioning input in process arguments, while the shared NTOWF helpers wipe their allocated password conversions and intermediate NT hash; focused CLI coverage checks output, source conflicts, input limits, line handling, and binary control bytes. When the CLI target is available, the default-on CMake `server` component and preview distro packages include this provisioning tool; explicit-off component-install coverage proves that it is omitted with NTLM. Focused identity tests cover accepted equivalent forms and malformed/ambiguous rejection; real local-PAM, Samba AD/SSSD, and joined FreeIPA/SSSD profiles cover accepted login, wrong-password rejection, locked-account, enforcing AD-GPO, or HBAC-policy rejection, managed-session cleanup, AD supplementary-group resolution, and IPA host-keytab validation.
- Integrated `frdpd` now generates a per-peer correlation id, includes it on accept/auth/logon/activate/disconnect logs, and sends it in `frdp-authd` IPC auth requests; `frdp-authd` uses a valid incoming id for syslog audit events and falls back to a local UUID when the request omits or sends an invalid id.
- Auth broker IPC request and response payloads now use explicit little-endian/fixed-field wire encoding with exact payload-size checks; password-bearing request wire buffers and authorization-token-bearing response wire buffers are cleared after use.
- `frdp-sesmand` now exposes a guarded local `--socket` IPC server; integrated `frdpd` can open and close managed sessions through configured `session_socket` / `--session-socket=<absolute-path>` after authentication and static-channel policy checks pass.
- The canonical `FRDP_IPC_SESSION_REQUEST_V3` session-open handoff and shared `FRDP_IPC_SESSION_RESPONSE` now use explicit little-endian/fixed-field wire encoding with exact payload-size checks; authorization-bearing V3 request wire buffers are cleared after use.
- `FRDP_IPC_SESSION_CLOSE_REQUEST` and `FRDP_IPC_SESSION_DISCONNECT_REQUEST` now use explicit little-endian/fixed-field wire encoding for `frdpd`, `frdpctl`, and `frdp-sesmand` close/detach paths.
- `FRDP_IPC_SESSION_LIST_RESPONSE` and `FRDP_IPC_SESSION_RELOAD_RESPONSE` now use explicit little-endian/fixed-field wire encoding with exact payload-size checks; their request messages remain header-only.
- Per-session agent input, frame metadata, and resize IPC messages now use explicit little-endian/fixed-field wire encoding with exact metadata payload-size checks; frame pixels remain a bounded raw byte tail after `FRDP_IPC_AGENT_FRAME_RESPONSE` metadata.
- Shared FRDP IPC clients now set send/receive timeouts, use `MSG_NOSIGNAL` when available so disconnected IPC peers return errors instead of terminating the process with `SIGPIPE`, and encode/decode the common IPC header as fixed little-endian `type`/`payload_len` fields.
- Session IPC now passes session ids and correlation ids into `frdp-session-agent`, which includes them in startup and display-backend exit logs.
- `frdp-session-agent` now reports readiness only after the selected Xvfb or Xorg dummy backend `exec()`, authenticated X display open, initial geometry, and XTest checks pass, and `frdp-sesmand` fails session creation if agent readiness is not reported. Xorg executable/config paths and every parent are root-owned, non-symlink, and non-writable by group/other; the manager validates them before startup/reload and the user agent repeats validation immediately before launch. Every Xvfb/Xorg backend receives a random per-session MIT-MAGIC-COOKIE-1 through an unlinked authority inode and disables TCP.
- Session IPC now creates a root-owned per-session agent control socket, returns the path to `frdpd`, forwards keyboard/mouse/Unicode/synchronize input events to `frdp-session-agent`, and has the agent validate root peer credentials plus session ids before injecting scancode keyboard, strict UTF-16 text including supplementary-plane surrogate pairs, and mouse events into Xvfb through XTest/X11.
- `frdpd` now pulls bounded raw framebuffer tiles from the session agent over the same root-only control socket, the agent uses XDamage-backed dirty-tile state to return selected dirty tiles or clean-tile responses when available, `frdpd` suppresses unchanged tiles with per-peer hashes, and changed tiles are sent to the RDP client as raw FreeRDP bitmap updates or negotiated minimum-color-loss/no-subsampling NSCodec `SurfaceBits` when the encoded tile is smaller.
- `frdp-sesmand` session IPC now performs POSIX account/group lookup, verifies the bounded group payload from the signed handoff, applies it with `setgroups()` before uid/gid drop, owns the PAM session, and cleans up agent process groups on close requests.
- `frdpd` now negotiates `drdynvc` only for a policy-approved Display Control handler, validates and queues monitor layouts from the DISP worker to the peer thread, asks the session agent to resize through XRandR, and updates peer geometry only after backend confirmation. The configurable Xorg dummy backend applies exact single-output modes with rollback; the local real-client profile proves `800x600 -> 1024x768 -> 800x600` display churn and synchronizes the retained display back to `1024x768` on reconnect. Fixed-mode Xvfb remains the default compatibility path and rejects different geometry.
- `frdpd` now uses a bounded raw-tile output scheduler with a shorter active-peer wait interval, a two-tile pump cap, a pump time check, and a reduced frame IPC timeout to improve initial/full-refresh and dirty backlog progress while keeping the slow-agent timeout budget comparable to the prior single-tile path.
- `frdpd` now enforces configured `max_connections` before authentication, channel checks, or managed-session creation; it can be set from `frdpd.toml` or overridden with `--max-connections=<n>`.
- `frdpd` now applies configurable static virtual-channel filtering during client capability processing; `[channels].static_mode` supports blocklist/allowlist semantics, defaults to empty blocklist mode, uses exact `static_deny` / `static_allow` lists, validates raw `CHANNEL_DEF` names fail-closed, permits `cliprdr` only for configured text mode, rejects unimplemented static channels plus `rdpsnd`/`rdpdr`, and permits `drdynvc` only when the configured dynamic policy authorizes the implemented `disp` handler. `[channels].dynamic_mode` plus `dynamic_deny` / `dynamic_allow` is parsed as exact-name policy, with the short `disp` policy name mapped to `Microsoft::Windows::RDS::DisplayControl` at the runtime authorization boundary. The text-only server `cliprdr` handler accepts `CF_UNICODETEXT`, strictly validates/converts UTF-8 and UTF-16, enforces configured direction/size limits, denies files/images/arbitrary formats, and uses bounded explicit IPC to an X11-selection bridge in the root-owned session agent. The WTS authorization hook denies unsupported server-created DVC names before `CREATE_REQUEST`; the peer opens `drdynvc` only after client join, manages the DISP context and queues layouts across the channel/peer threads. Focused config/codec/conversion tests and local real-client Display Control plus bidirectional supplementary-plane Unicode clipboard coverage exist. Other useful channel handlers are still not implemented.
- `frdpd`, `frdp-authd`, `frdp-sesmand`, `frdp-session-agent`, and `frdpctl` now escape client/IPC/CLI/environment-supplied output fields before writing peer, auth, channel, session, helper lifecycle, or admin CLI events; password payloads remain unlogged.
- `frdpctl` now escapes IPC-supplied session ids, users, display names, session-manager errors, and session-manager socket paths before writing them to the admin terminal.
- Shared FRDP IPC client sockets and `frdp-authd` / `frdp-sesmand` listener plus accepted IPC client sockets are now created close-on-exec where supported, with `fcntl(FD_CLOEXEC)` fallback; the `frdp-sesmand` per-session agent control socket remains intentionally inheritable for `frdp-session-agent` startup.
- `frdp-authd`, `frdp-sesmand`, and per-session agent control listener startup now refuse to unlink an existing live Unix socket path; stale same-owner socket nodes are removed only after a nonblocking live-listener probe and same-inode recheck.
- `frdp-authd` and `frdp-sesmand` reject oversized inbound IPC request payload lengths before type-specific request handling while keeping their service loops alive.
- Managed session opens now use `FRDP_IPC_SESSION_REQUEST_V3` with a short-lived HMAC auth token issued by `frdp-authd` after successful authentication. The token payload is length-prefixed and covers the authenticated user, remote host, correlation id, POSIX uid/gid, bounded supplementary groups, account-present state, nonce, and expiry; `frdp-sesmand` verifies those fields, consumes accepted nonces once, rejects mismatched POSIX group payloads, and rejects legacy V1/V2 session-open requests before body decode.
- `frdp-sesmand` display allocation skips display numbers already present in its session registry or visible through local X lock/socket paths, and selected displays are held with FRDP-owned atomic reservation files for the session lifetime. Linux reservations store a versioned owner PID plus `/proc` start time, reconcile both disappeared owners and PID reuse, retain fail-closed legacy PID-only parsing, and use same-inode checks before unlink. Durable session records now bind the reservation and agent socket inode identities so restart cleanup cannot unlink replacements.
- `frdp-sesmand` has explicit state, cleanup and reconnect policy helpers, records hard close through `ACTIVE` -> `STOPPING` -> `DEAD`, accepts normal peer disconnect as a durable `DISCONNECTED` transition, and requires valid V3 authorization before reconnect. Versioned metadata is committed before successful open/detach/reconnect responses; Linux restart reconciliation opens a stable pidfd, verifies PID/start time/effective UID/PGID, signals only that agent through the pidfd, requires its process group to disappear, and removes matching runtime artifacts. Restart does not restore reconnectable sessions or the lost PAM handle, so logind/cgroup-backed full lifecycle recovery remains open.
- `frdp-sesmand` now handles SIGINT/SIGTERM by stopping the IPC accept loop and cleaning up tracked agent process groups, PAM sessions, and runtime sockets before exit.
- `frdpd`, `frdp-authd`, `frdp-sesmand`, and `frdp-session-agent` install explicit non-restarting SIGINT/SIGTERM handlers so stop requests are observed by their main loops and fail closed if handler installation fails; `frdp-authd` also closes and unlinks its IPC socket on graceful stop, and a running `frdp-session-agent` terminates its display backend on stop.
- Focused CTest coverage starts live `frdp-authd` and `frdp-sesmand` helpers, verifies role-bound health responses and separate health rate limits, proves health exhaustion does not consume normal request capacity, kills `frdp-authd` with `SIGKILL`, proves health fails during the outage, restarts it on the same stale socket path, and proves health plus normal negative-path IPC recover. When `libpam-wrapper` is available, a blocking test-only PAM module additionally proves an auth request already inside `pam_authenticate()` terminates with a bounded IPC failure after `frdp-authd` is killed. A slow-drip unit test verifies the startup exchange has one absolute deadline. The installed listener/helper units use `Restart=on-failure`; `frdpd.service` uses `Wants=` so a helper restart does not tear down established listener state, while bounded startup health still rejects missing helpers before listening.
- The Samba AD/SSSD and joined FreeIPA/SSSD Compose profiles now deterministically kill the exact `frdp-sesmand` serving an identified active provider-backed session, require a replacement manager PID and socket inode, prove the old agent and durable runtime state are removed, and complete a fresh SSSD-authenticated disconnect/reconnect cycle. `frdpd` tolerates two transient framebuffer IPC failures but continues a lightweight agent probe even while RDP output is suppressed and closes a stale peer after the third consecutive failure, preventing a client from remaining attached to a desktop whose session manager and agent are gone.
- The Debian systemd lifecycle gate now has an additional Samba AD/SSSD mode. It installs the real DEB into a privileged Ubuntu 24.04 container, joins that host to a separately provisioned DC, verifies SSSD account and supplementary-group policy, and then repeats active-session manager recovery, in-flight broker recovery, bounded helper outages, upgrade, downgrade rollback, remove, and purge. The DC links a remote-interactive allow/deny GPO, the package host runs SSSD GPO enforcement for `frdpd`, and an enabled policy-test user with a valid NTLM proof must reach PAM, be denied, and leave no session artifacts at the base, upgraded, and rolled-back package versions. Each transition requires the held provider-backed client and old agent to disappear, all packaged service invocations to change, the SSSD invocation and join to remain valid, and a fresh allowed domain login/reconnect to pass.
- Integrated `frdpd` now zero-frees the peer-owned FreeRDP auth identity immediately after `frdpd_authenticate_identity()` returns and the brokered result has been copied into `frdpd` session context.
- WinPR SAM parsing now strictly rejects non-hex LM/NT hash fields and exposes full-store validation with secure per-entry/read-buffer cleanup; focused coverage repeats valid validation, malformed validation, and malformed lookup through one open SAM handle to verify parser recovery.
- `frdp-authd` now releases its locked password copy immediately after `pam_end()` and before NSS/SSSD account lookup, while WinPR securely clears both the RC4 provider wrapper and the internal key-derived permutation state before freeing them. PAM-module-owned response storage remains outside application ownership.
- NTLM proof binding now extracts only the delegated user/domain fields instead of creating an unused password copy.
- NTLM proof binding now accepts the domain retained only in FreeRDP's authenticated proof when delegated password credentials contain the same short user, while still requiring every delegated domain to be present and equal in the proof; the Samba AD/SSSD profile exercises this real-client form.
- Strict-warning CI now exercises both the implicit default-on `WITH_FRDPD_NTLM` configuration and an explicit build-time-disabled configuration, asserts the resolved CMake cache value, and runs the focused FRDP suite in both variants. `TestFreeRDPFrdpd` verifies that the disabled build rejects runtime NTLM fallback while the default build reaches the normal certificate/SAM policy path.
- CMake now generates and installs `frdpd.toml` from the selected NTLM build feature: the default-on build enables fallback and its protected SAM path, while an explicit `WITH_FRDPD_NTLM=OFF` build disables fallback and omits the active SAM path. Both generated samples are parsed and exercised by their matching focused build, and component-install checks verify the installed result.
- Successful broker authentication now binds session identity to the post-account `PAM_USER` and canonical NSS/SSSD `pw_name`: auth-response V2 carries that bounded name back to `frdpd`, and supplementary groups, signed authorization token, reconnect identity, and V3 session open all use the same value. Missing, empty, control-containing, overlong, unterminated, or NSS-unresolvable identities fail closed. Focused PAM-helper, explicit IPC codec, fuzz-decoder, and broker-adapter tests cover extraction and canonical-name handoff.

## Remaining issues

| ID | Issue | Severity | Confidence | Evidence | Impact | Recommended action |
|---|---|---:|---:|---|---|---|
| FRDP-001 | Helper binaries, canonical helper startup, and real-client local/AD/IPA login coverage exist, but deployment-level recovery coverage is still incomplete. | 93% | 100% | `server/frdp/CMakeLists.txt` builds helper targets and install rules under `WITH_FRDPD`; normal `frdpd` startup requires healthy auth/session IPC roles and operations fail closed while either helper is unavailable. Signed short-lived auth tokens bind V3 session opens to POSIX uid/gid/groups/account state, the old peer-worker PAM fallback has been removed, and `frdp-authd` bounds repeated failures independently by account and remote source before PAM. Focused tests cover the health codec, both live helper roles, auth failure limiting, direct helper crash/restart, and deterministic broker/session-manager termination inside PAM auth/open/close calls. A root-gated test runs both helper binaries as transient systemd services with `Restart=on-failure`, kills each main process, and requires a replacement socket inode plus the correct role-bound health response on the same path. The installed Debian lifecycle gate also covers active-session `frdp-sesmand` termination, in-flight packaged `frdp-authd` termination, bounded helper outages, endpoint replacement, cleanup and relogin. Its Samba AD/SSSD mode proves the same installed-unit recovery while preserving the live SSSD invocation and domain join. The Samba AD/SSSD and joined FreeIPA/SSSD Compose profiles additionally identify an active provider-backed session, kill its exact manager, require peer disconnect plus manager endpoint replacement and old-agent/runtime cleanup, and complete a fresh SSSD login/reconnect cycle. | Canonical helper login/session behavior now has real-client recovery evidence across both SSSD providers plus transient-service-manager and installed-unit coverage, but the failure limiter is process-local, and longer operational outages and repeatable CI history remain absent. | Promote the current gates to repeated distro CI, then cover longer partition/outage windows across additional provider/distribution combinations. |
| FRDP-002 | IPC connects `frdpd` to auth, token-bound session open, agent input injection, and a resizable raw framebuffer path, but Kerberos helper IPC and production framebuffer handling are not implemented. | 52% | 95% | Managed V3 session responses include an agent control socket and `frdpd` forwards input events, framebuffer tile requests, and policy-approved Display Control layouts to it. The Xorg dummy backend applies exact single-output RandR modes with rollback, capture-state refresh, and post-connect geometry synchronization. Root-live coverage proves two geometry changes and the local real-client profile proves DVC-driven `800x600 -> 1024x768 -> 800x600` churn plus reconnect. Bounded raw-tile scheduling and opportunistic NSCodec exist, but no code path invokes `frdp-krb-authd`, and RFX/RDPGFX production codec policy is not implemented. | The intended multi-process auth/session lifecycle is operational for the covered password path, but Kerberos-first auth and a production-quality graphics path remain unavailable. | Add production codec selection/encoder scheduling, multi-monitor and load/soak evidence, then integrate Kerberos helper IPC. |
| FRDP-003 | The integrated `frdpd` peer-worker PAM fallback has been removed. | 100% | 100% | Normal startup requires both `auth_socket` and `session_socket`; without helper sockets, builds reject startup in `server/frdp/frdpd/frdpd.c`. `server/frdp/CMakeLists.txt` no longer exposes `WITH_FRDPD_IN_PROCESS_PAM`, `frdpd` no longer accepts `--allow-in-process-pam` or `--no-pam-session`, and `server/frdp/frdpd/frdpd_auth.c` only authenticates through the configured `frdp-authd` IPC socket. The `frdpd` peer context and auth result no longer carry PAM handles, credential state, or PAM session-open state. `TestFreeRDPFrdpd.cmake` covers the removed legacy option as a fail-closed unknown argument. | The intended privilege boundary is no longer bypassable through a development build option. | Keep the explicit `frdpd --pam-auth-test` smoke path for local PAM checks and keep production login/session behavior on the helper topology. |
| FRDP-005 | Desktop data path remains incomplete: backend input injection, resizable raw framebuffer tiles, Display Control, text clipboard, basic output scheduling, opportunistic NSCodec compression, and channel policy exist, but production graphics handling and most useful channels are missing. | 66% | 100% | `frdpd` forwards input, framebuffer and bounded clipboard traffic over explicit agent IPC, conditionally opens joined `drdynvc`, and applies validated Display Control layouts on the peer thread. The local profile proves server caps, actual `800x600 -> 1024x768 -> 800x600` Xorg dummy geometry churn, reconnect synchronization, continued connection, and supplementary-plane Unicode clipboard exchange in both directions. Raw/XDamage capture and bounded raw/NSCodec output remain prototype paths; IME/layout-safe keyboard input, audio handlers, and RFX/RDPGFX policy are not implemented. | A client can receive visible output, resize a single-output desktop, reconnect, and use text clipboard, but performance and channel completeness remain insufficient for daily interactive use. | Add production codec scheduling, IME/layout-safe text input, audio, multi-monitor, and broader runtime interoperability/load tests. |
| FRDP-006 | Static/dynamic channel filtering, DVC authorization, Display Control transport, and policy-gated text-only clipboard are implemented, but audio and other useful channels are not. | 82% | 100% | The runtime gate permits `drdynvc` only when the `disp` dynamic policy authorizes the implemented handler and permits `cliprdr` only for active text policy. The server handler accepts only `CF_UNICODETEXT`, enforces direction/size limits, and exchanges bounded explicit IPC with the agent's X11 `CLIPBOARD` bridge. Config, IPC and Unicode conversion tests cover policy and malformed inputs; WTS tests cover pre-create denial; the local profile proves live `xfreerdp` Display Control and bidirectional Unicode clipboard transfer. Unsupported static channels, unknown server-created DVC names, files, images, audio and device redirection remain denied. | Two useful channel features have end-to-end transport and policy evidence; audio and broader client interoperability remain unmet. | Add baseline audio with explicit policy/tests and broaden clipboard/DVC interoperability across independent FreeRDP and Windows clients. |
| FRDP-007 | Linux `xfreerdp`, Samba AD/SSSD, and joined-host FreeIPA/SSSD interoperability are automated, but the broader client/provider matrix remains incomplete. | 93% | 100% | The local-PAM, machine-joined Samba AD/SSSD, and host-enrolled FreeIPA/SSSD Compose profiles exercise real TLS/NLA/CredSSP login, accepted and denied PAM outcomes, NSS/domain identity, graphical session activation, deterministic disconnect/reconnect, XWD capture, and cleanup with checked diagnostic artifacts. Samba provisions and links a security GPO, configures SSSD GPO enforcement plus `frdpd` remote-interactive mapping, and proves a supplementary-group allow alongside denial of a second enabled user before the real RDP denial. Both SSSD profiles additionally prove deterministic active-session manager crash cleanup, replacement endpoint identity, bounded stale-client disconnect, and a fresh post-recovery session/reconnect. FreeIPA requires a root-only host keytab containing `host/frdpd.ipa.test`, uses the IPA identity/auth/access providers with `krb5_validate=true`, disables `allow_all`, and proves both an explicit HBAC allow and an enabled-user denial before and around recovery. The profile also rotates that host principal server-side, requires an increased KVNO, proves the old key is rejected and the atomically installed root-only replacement obtains a ticket, then completes its PAM/SSSD RDP probes. Focused config and CLI tests continue to cover policy parsing and session control. Windows `mstsc`, Kerberos-only CredSSP, broader AD GPO scope/link precedence and trust/forest cases, IPA multi-master/ticket-renewal, codec/channel, and long-running interoperability scenarios remain unchecked. | The covered Linux client and provider baselines can still miss Windows-specific, Kerberos-first, broader policy/topology rotation, graphics, and sustained interoperability failures. | Promote current profiles to CI and add the Windows/provider/security/channel matrix as separate deterministic gates or external lab jobs. |
| FRDP-008 | `server/frdp/frdpd` consumes only a subset of `frdpd.toml`. | 56% | 100% | `server/frdp/frdpd/frdpd.c` applies `listen`, implemented `security=nla`, `tls_cert`, `tls_key`, `max_connections`, `mode=pam-sssd`, `pam_service`, absolute `auth_socket`, `ntlm_fallback` through CredSSP/Negotiate package selection plus fail-closed selected-package verification before PAM, absolute `[session].session_socket`, static/dynamic channel policy, and `[clipboard]` text mode/direction/size at peer startup. It accepts `--max-connections=<n>` as a CLI override and atomically reloads channel/clipboard policy for future peers on `SIGHUP`, retaining active-peer snapshots and the last valid policy after parse failure. `server/frdp/config/frdp-config.c` rejects unsupported `security` and `auth.mode` values, rejects relative helper socket paths, validates Kerberos identity fields as a strict `kerberos=true` set that requires `ntlm_fallback=false`, an absolute keytab path, and a `TERMSRV/fqdn` accepted SPN, while `frdpd` still fails closed on that mode until the integrated acceptor exists. It accepts `[audit]` only when `enabled` is omitted or `false`, rejects `enabled=true`, and rejects unknown session/channel/clipboard/audit fields. | Kerberos acceptor integration, reload for non-channel fields, useful channels beyond Display Control/text clipboard, and active audit config remain unavailable until enforcement exists, but they no longer appear as silently accepted active policy. | Extend config parsing and application incrementally, failing closed on invalid values and documenting unsupported fields until they are wired. |
| FRDP-009 | Packaging remains partial, but both server-only preview formats have dependency-resolved clean build/install/linkage coverage and a documented signing/reproducibility contract. | 90% | 100% | Component-install CTests prove that `winpr-hash` is included for default-on NTLM and omitted for an explicit-off build. Debian and RPM builds disable unrelated client/multimedia features and carry their exact FreeRDP/WinPR libraries in ABI-private directories. A clean Ubuntu 24.04 declared-dependency build produced the Debian package without FFmpeg runtime dependencies or public bundled-library `shlibs`/`ldconfig` metadata; a second clean container passed APT install, `dpkg -V`, private linkage for all six binaries, installed `winpr-hash` provisioning, and purge through generated maintainer scripts. The binary package now carries scoped DEP-5 metadata for its runtime build closure, including non-Apache exceptions, while a complete source-package audit remains open. All six packaged commands have installed manuals, daemon units carry resolvable `Documentation=man:` links, and a fresh pedantic lintian run reports only the expected first-upload changelog warning; the clean builder retains that report on both success and failure and rejects error-level policy violations. Debian maintainer-script and clean-install checks prove that the unprovisioned listener and helpers create no enablement links or daemon processes; a hermetic final-maintscript test covers active/inactive upgrade, policy denial, alternate-root isolation, remove, and purge. A privileged Ubuntu systemd-container gate additionally installs the real package, provisions TLS and an NTLM SAM through the packaged tool, starts the unmodified packaged units, verifies their real PIDs, role-bound authd startup health, sesmand control IPC, and TCP listener over synthetic upgrade plus downgrade rollback, and checks conffile, enablement, administrator-secret, removal, and purge behavior. An independently packaged Ubuntu FreeRDP 3.5 client completes local-PAM managed-session open, detach, stable-id/display/PID reconnect, and cleanup against the base, upgraded, and rolled-back package. Held local-PAM client probes additionally prove bounded disconnect and removal of the old registry entry and session-agent process during both upgrade and rollback before post-transition login. A second lifecycle mode joins the installed-package host to Samba AD, verifies SSSD account and supplementary-group policy, and proves provider-backed active-session cleanup plus fresh login/reconnect across the same upgrade and rollback transitions while the SSSD service and machine join remain stable. The corresponding Fedora 42 `dnf builddep`/`rpmbuild -ba`, DNF install, `rpm -V`, private dependency-metadata, and linkage checks also pass. Successful package-job history, additional distributions/architectures, complete source copyright review, signing/repositories, and active MAC enforcement review remain open. | Packages can still fail on other distributions, architectures, or SSSD providers beyond Samba AD; release keys/repositories are not configured, and installed draft policy examples are not sufficient production confinement. | Accumulate both package gates, complete source copyright review, broaden the distro/architecture matrix, then add signing/repository automation and production MAC enforcement review. |
| FRDP-011 | Audit correlation is partial, not end-to-end. | 40% | 95% | `server/frdp/frdpd/frdpd.c` generates a per-peer id and logs accept/auth/logon/activate/disconnect plus static channel policy denials with it; NLA logon now records the selected SSPI package name under the same correlation id when the package can be queried. Client-supplied hostnames, authenticated usernames, static channel names, and IPC-supplied session ids, display names, agent socket paths, and session-manager error strings are escaped before peer/channel/session logging. `server/frdp/frdpd/frdpd_auth.c` sends the id in `FRDP_IPC_AUTH_REQUEST_V2`; `server/frdp/frdpd/frdpd.c` sends it in session open/close IPC; `server/frdp/frdp-authd/frdp-authd.c`, `server/frdp/frdp-sesmand/frdp-sesmand.c`, and `server/frdp/frdp-session-agent/frdp-session-agent.c` use ids in auth/session/agent audit events. `server/frdp/config/frdp-config.c` now recognizes `[audit]` only as an explicitly disabled stanza and rejects `enabled=true` or unknown audit keys until structured sinks are enforced. Structured active audit config and useful channel handler decisions are not implemented yet. | Current auth/session/agent startup logs, selected NLA SSPI package evidence, and static channel denials can be joined on helper IPC paths, but future channel data decisions still cannot be correlated end-to-end. | Carry the same id through structured audit configuration and useful channel handler policy logs before marking audit correlation complete. |
| FRDP-012 | `frdp-sesmand` has a session service endpoint and controlled real-client reconnect, but restart recovery and production lifecycle management are incomplete. | 94% | 100% | Authorized V3 open, detach, explicit/implicit reconnect and hard close are implemented with PAM ownership, identity/resource guards and process-group cleanup. Versioned atomic metadata records PID/start time/effective UID/PGID, lifecycle state, display, and socket/reservation inode identities; open and state-change responses require a committed record. Linux startup scans fail closed on malformed or uncertain records, pin the candidate with pidfd before identity checks, signal only that agent, require its process group to disappear, and remove only matching artifacts. Active and disconnected agents are supervised over private inherited socketpairs with dedicated responder threads, progress watchdogs, absolute nonce-echo deadlines and consecutive-failure cleanup. Focused/live lifecycle tests, transient-systemd and installed Debian-unit recovery gates, local-PAM reconnect, and separate Samba AD/SSSD plus joined FreeIPA/SSSD active-session crash/endpoint-replacement/cleanup/fresh-session cycles pass. The installed Debian Samba AD/SSSD mode repeats active-session manager recovery and upgrade/rollback cleanup with unchanged provider service identity and fresh domain reconnects. `frdpd` closes a stale peer after three consecutive framebuffer IPC failures once the old agent is gone. | Restart cleanup is durable for the covered process and runtime artifacts under focused, both provider-backed Compose, and packaged-unit tests, but restart deliberately does not restore reconnectable sessions and cannot close a PAM handle lost with the old manager. Reconnect across restart, Windows-client interoperability, logind/cgroups, and longer operational outage policy remain open. | Move process/PAM ownership into logind/systemd scopes or cgroups, add full PAM reconciliation and reconnect-across-restart policy, and add longer-outage plus Windows-client/provider crash interoperability gates. |
| FRDP-013 | Display allocation and single-output resizing work, but production ownership and multi-monitor behavior remain prototype-level. | 76% | 100% | Display allocation/reconciliation remains inode- and process-identity-bound. The configurable Xorg dummy backend uses trusted absolute paths, disables TCP listening, selects exact RandR modes, rejects multi-output CRTCs, rolls back failed changes, and refreshes framebuffer geometry. Root-live coverage proves `1024x768 -> 800x600 -> 1280x720`; local real-client coverage proves repeated resize churn and reconnect synchronization. Fixed-mode Xvfb remains available. | Runtime-directory loss, true multi-monitor mapping, Windows-client churn, backend load/soak, and full PAM/logind recovery are not covered. | Add logind/cgroup ownership, runtime-directory recovery policy, multi-monitor design, and broader client/load evidence. |
| FRDP-014 | `frdp-session-agent` provides Xvfb and resizable Xorg dummy backends, validated input, raw/XDamage framebuffer tiles, and text clipboard, but lacks production framebuffer/audio features. | 65% | 100% | The agent verifies backend `exec()`, authenticated X display and XTest readiness, accepts root-only session-bound IPC, and permits request correlation ids to rotate across reconnect. Complete Xorg paths are root-owned/non-writable/non-symlink and revalidated by the agent; launch uses an unlinked random per-session Xauthority and disables TCP. Resize selects one connected output and an exact mode, rejects shared CRTCs, stages/rolls back root size, and refreshes actual root-window capture geometry. The root-gated live test proves another UID cannot open the display, then changes geometry twice before frame/clipboard/supplementary-Unicode checks; local E2E proves repeated real-client churn and reconnect geometry synchronization. Xvfb remains fixed-mode compatibility. | Basic interactive behavior is demonstrable, but graphics performance, IME/layout handling, audio, multi-monitor, and broad client compatibility remain prototype-level. | Add production encoder scheduling/codec policy, IME/layout-safe input, audio, multi-monitor, and broader client/load tests. |
| FRDP-015 | `frdp-krb-authd` is a build-only skeleton, not a working Kerberos/CredSSP acceptor. | 76% | 100% | `server/frdp/frdp-krb-authd/frdp-krb-authd.c` now disables core dumps before touching keytab/GSS credential material and base64-decodes its standalone token argument before `gss_accept_sec_context()`, but `frdpd` does not extract or pass real CredSSP SPNEGO tokens to it; the helper remains build-only and is intentionally not installed. It preserves an existing non-empty `KRB5_KTNAME`, otherwise uses `FRDP_KRB_KEYTAB` before falling back to `/etc/frdpd/frdpd.keytab`, can import `FRDP_KRB_ACCEPTOR_NAME` as a host-based service name and acquire acceptor credentials for it, rejects contexts that set `GSS_C_DELEG_FLAG` while releasing any delegated credential handle, tries exact displayed-principal NSS lookup first for valid simple principals, then fail-closed normalizes simple `user@REALM` principals while rejecting service/instance and malformed principals, and performs bounded supplementary-group lookup for the mapped POSIX account. `frdpd.toml` validates Kerberos identity fields but `frdpd` fails closed when `kerberos=true` is requested until the integrated acceptor path exists. Integrated `frdpd` rejects unsupported Remote Credential Guard ticket handoff instead of allowing a default-success callback path. `TestFreeRDPFrdpKrbAuthd` covers no-core hardening, keytab environment selection, acceptor-name selection/import, delegated-credential flag rejection, bounded group lookup for the current account, accepted/rejected base64 token decoding, and principal normalization cases when the GSSAPI target is available. | Kerberos-first authentication still cannot be validated, and real `frdpd` SPNEGO transport plus SSSD-backed enterprise principal/account/session binding and delegation policy review are not integrated. | Implement SPNEGO token ingestion from CredSSP, SSSD-backed principal mapping, PAM account/session integration, and integrated delegation-policy enforcement before installing/package-enabling it. |
| FRDP-016 | Integrated secret hardening is partial. | 89% | 95% | `server/frdp/frdpd`, `server/frdp/frdp-authd`, `server/frdp/frdp-sesmand`, and standalone `server/frdp/frdp-krb-authd` fail closed if core dumps/non-dumpable process hardening cannot be applied on their sensitive paths. `server/frdp/frdp-authd` locks-or-fails-closed and wipes broker-side auth IPC password copies, locks and wipes temporary PAM password buffers, wipes broker-side PAM conversation response copies on allocation-failure cleanup, wipes authorization-token response structs after IPC send, wipes local authorization-token/group staging buffers, and uses explicit volatile wiping before unmapping locked buffers. `server/frdp/frdpd/frdpd_auth.c` performs NTLM proof binding from user/domain only without duplicating the delegated password, locks-or-fails-closed and wipes temporary CredSSP password copies, the auth-IPC request password field, the received auth-token response staging struct, and auth-result token/group fields before releasing or reinitializing them. Integrated `frdpd` wipes managed-session authorization handoff request/response staging after session-open handling plus managed-session close/rollback/retry IPC staging, and securely clears copied auth-token/group state after successful auth-result handoff, after successful session-open token consumption, and during failure/peer-context cleanup. `frdp-sesmand` wipes received session handoff request/authorization staging after accepting or rejecting the session request, wipes verified auth-token nonce staging after consume/reject, securely clears expired consumed-token cache entries, and wipes session/list/reload response staging after IPC send. Standalone `frdp-krb-authd` wipes decoded input-token buffers and POSIX group staging buffers before release. The server-side core now wipes/removes the `rdpSettings` password duplicate immediately after copying the callback identity, wipes plaintext/decrypted `TSCredentials` immediately after encode/decode, and wipes received encrypted `authInfo` immediately after decode; WinPR also securely wipes full parsed SAM buffers, decoded LM/NT hashes, NTLM message fields, and inline NTLM context key material before release. Focused coverage proves the identity survives while the settings password is removed, exercises successful plus failed encode/encrypt/decrypt/decode cleanup paths, and verifies complete SAM-entry reset semantics. Integrated `frdpd` also calls `sspi_FreeAuthIdentity()` on the peer-owned FreeRDP auth identity immediately after the authentication callback has copied the brokered result into `frdpd` session context. `libfreerdp/core/nla.c` wipes Remote Credential Guard Kerberos ticket-logon buffers and their encoded staging stream before release, and the Kerberos SSPI path wipes a forwarded-TGT buffer on a local packaging failure. The auth broker path is required for normal startup, and the explicit in-process PAM fallback build option/CLI path has been removed. | Password material lifetime in FreeRDP-owned staging and the WinPR SAM-backed NTLM mechanism is shortened, but material may still exist in other SSPI mechanism internals, PAM-owned successful conversation response storage, and Kerberos/SPNEGO internals. | Keep scoped locked buffers for any remaining temporary copies and audit SSPI/GSSAPI/PAM credential ownership. |

## Build observations

- A versioned fixed-width session-metadata store has focused coverage for strict record validation, trusted-directory and regular `0600` file checks, exact-length decode, atomic create/update with file and directory synchronization, stale temporary-file cleanup, malformed/symlink fail-closed behavior, and same-inode removal. It is wired into open/detach/reconnect/cleanup, and the prerequisite-gated service test now kills `frdp-sesmand` with `SIGKILL`, verifies the agent survives the manager, restarts on the same runtime directory, and verifies pidfd-directed agent shutdown, process-group disappearance, agent-socket, display-reservation and metadata cleanup. Abruptly lost PAM handles still cannot be closed by the replacement process.
- Agent heartbeat IPC has an explicit fixed-width little-endian nonce wire format and a private inherited `SOCK_SEQPACKET` transport independent of frame/input contention. A focused timeout/resume test proves a stale whole response is discarded by nonce without desynchronizing the next probe. Config parsing covers defaults, bounds, cross-field timeout validation, and duplicate rejection; a prerequisite-gated PAM/Xvfb test first occupies the data control path without losing the session, then stops the agent and verifies threshold-driven process-group and runtime-artifact cleanup.
- `TestFreeRDPFrdpProcessIdentity` covers the shared Linux PID/start-time/effective-UID reader, while `TestFreeRDPFrdpDisplayPolicy` explicitly covers live and dead v2 reservation owners plus the conservative legacy and exact-identity PID 1 policies.
- The integrated `frdpd` target and helper targets build successfully in `/tmp/opencode/freerdp-current-build`.
- The isolated `server` component install check succeeds for FRDP binaries/config when unrelated shadow/proxy server targets are disabled.
- The focused `freerdp`, `freerdp-server`, `frdpd`, `frdp-authd`, `frdp-sesmand`, and `frdp-session-agent` targets build successfully after the XTest input, raw framebuffer tile, unchanged-tile suppression, XDamage clean-tile, dirty-tile scheduling, XRandR resize-request handling, bounded raw output scheduling, opportunistic NSCodec `SurfaceBits`, strict UTF-16 text input including supplementary-plane surrogate pairs, static channel filtering, static/dynamic channel validation, policy-gated `drdynvc`, Display Control DVC lifecycle, DVC authorization hook and runtime deny-path coverage, `frdpd` VCM wiring, `max_connections`, log-field escaping, scoped `frdpd` password-copy locking, authd PAM-response failure-cleanup wiping, authd interactive and duplicate-password PAM prompt rejection, live-helper socket collision protection, peer auth-identity cleanup, token-bound session-open changes, POSIX-account/group-bound auth-token changes, and in-memory session state/cleanup/reconnect-selection policies, plus explicit IPC header, auth broker payload, session handoff/close/response, session list/reload response, and agent metadata wire-format changes.
- The `TestFreeRDPFrdpConfig` CTest covers the shipped sample config, implemented server/auth/session/audit config fields, static/dynamic channel config parsing, default empty blocklist behavior, exact allow-list and deny-list matching, malformed config rejection including overlong lines, duplicate/unknown section/key rejection including clipboard scalar keys, unsupported `security` and `auth.mode` rejection, unsafe helper socket rejection, policy-gated `drdynvc`, Display Control runtime mapping, `CHANNEL_DEF` validation, `max_connections` parsing, enforced `ntlm_fallback` parsing, and Kerberos identity field validation including unsafe keytab paths. `TestFreeRDPFrdpAuthdPam` covers the auth broker's non-interactive password conversation response plus fail-closed rejection of duplicate password prompts, interactive/unsupported prompt styles, and oversized conversation arrays, and `TestFreeRDPFrdpPam` covers the in-process PAM adapter's user/domain normalization rules, PAM authentication/account status mapping, non-interactive conversation handling for a single password prompt, informational messages, error messages, duplicate password prompt rejection, interactive/unsupported prompt rejection, malformed callback arguments including oversized conversation arrays, and deterministic auth/account/session lifecycle branches through a test-only PAM-ops harness.
- The focused FRDP CTests cover secure explicit IPC codecs, including bounded clipboard SET/GET messages; strict UTF-8/UTF-16 clipboard conversion and malformed input rejection; stateful supplementary-plane input decoding, exact X11 Unicode KeySym mapping, and root-only live X11 key-event delivery; policy direction/size decisions; active clipboard-config daemon startup; helper lifecycle and malformed clients; session-agent peer validation, resize/frame behavior and root-only live X11 clipboard roundtrip; PAM/session lifecycle, channel policy, Display Control, systemd/tmpfiles/monitoring contracts, and the previously documented auth/session/frame/input/display surfaces. The local real-client profile additionally proves supplementary-plane Unicode clipboard transfer in both directions before disconnect/reconnect cleanup.
- Reconnect candidates are matched against both the canonical account name and the UID verified by the V3 authorization token, so a recreated same-name account cannot inherit a disconnected session owned by the previous UID.
- `TestFreeRDPFrdpSessionIdentity` covers root `setgroups` -> `setgid` -> `setuid` ordering and syscall failures plus exact real/effective UID, GID, and supplementary-group matching for non-root same-user development sessions; a local standalone smoke run also verified Xvfb/agent startup followed by PAM and process cleanup on SIGTERM.
- With Xvfb and `/etc/pam.d/common-session-noninteractive` available, `TestFreeRDPFrdpServiceIpc` performs a live authorized V3 session open, validates the active registry entry, detaches it, validates `DISCONNECTED`, implicitly reconnects with a fresh token while preserving session id/display/agent socket/PID, hard-closes it, and verifies empty registry plus process/socket cleanup.
- When run as root, `TestFreeRDPFrdpServiceIpc` opens the real `frdp-authd` and `frdp-sesmand` sockets from a child dropped to an unprivileged UID, verifies the typed `unauthorized IPC peer` response from both helpers, restores the isolated fixture permissions, and proves each unchanged helper still answers health plus its role-specific post-rejection request check.
- With `libpam-wrapper`, dedicated blocking session modules let `TestFreeRDPFrdpServiceIpc` kill the real `frdp-sesmand` inside both `pam_open_session()` and `pam_close_session()`. The waiting V3 clients receive bounded IPC failures. The open-crash case proves no session entry, agent socket, display reservation, or durable metadata was allocated; the established-close case proves the agent process group is already gone while durable `STOPPING` metadata, socket, and reservation remain, then restarts the manager and verifies complete reconciliation of those artifacts.
- The real-client `local`, Samba AD/SSSD, and joined FreeIPA/SSSD Compose profiles disable automatic client reconnect, require and clean the managed session created by the successful auth-only probe, require the wrong-password and locked-account/AD-GPO/HBAC-policy probes to leave both the session registry and durable session runtime directory empty, then require the first graphical `xfreerdp` connection to become `active`, detach it, connect a second client, and prove exclusive reattach with the same session id, display and agent PID before a second detach and explicit cleanup. Each profile starts with fresh profile containers, volumes, and artifacts. The local profile requires exactly five PAM accepts: the baseline authentication probe, initial graphical session, two single-attempt channel-policy denials after successful PAM, and reconnect. Samba and FreeIPA require six because each additionally authenticates an active-session crash probe and a full post-recovery session/reconnect cycle through SSSD. All profiles require one wrong-password NTLM MIC rejection before PAM, two named denied-user PAM/SSSD outcomes from that probe's connection path, and no NTLM proof/delegated-identity mismatch. Samba additionally retains exact DC GPO fixture and enforcing SSSD allow/deny evidence.
- The broad build still emits existing FreeRDP deprecation warnings unrelated to the new PAM/SSSD/NLA prototype, while the focused FRDP warning build treats daemon/helper/test warnings as errors through `WITH_FRDPD_STRICT_WARNINGS=ON`.
- A focused ASan+UBSan build with `WITH_SANITIZE_ADDRESS=ON`, `WITH_SANITIZE_UNDEFINED=ON`, explicit ASan/LSan/UBSan runtime options, and `BUILD_TESTING=ON` was verified through the full `^TestFreeRDPFrdp` CTest set after fixing the session-agent stop-path dirty-tile leak it exposed, and the FRDP workflow now includes the same focused sanitizer build/test gate with sanitizer-log artifacts.
- `TestFuzzFreeRDPFrdpConfig`, `TestFuzzFreeRDPFrdpAuthToken`, `TestFuzzFreeRDPFrdpAuthdPam`, `TestFuzzFreeRDPFrdpPam`, `TestFuzzFreeRDPFrdpNtlmIdentity`, `TestFuzzFreeRDPFrdpIpcCodec`, `TestFuzzFreeRDPFrdpClipboard`, `TestFuzzFreeRDPFrdpFramePolicy`, `TestFuzzFreeRDPFrdpInputPolicy`, `TestFuzzFreeRDPFrdpDisplayPolicy`, and `TestFuzzFreeRDPFrdpSessionLifecycle` build under `BUILD_FUZZERS=ON` with ASan/UBSan and smoke-run the FRDP config parser, static/dynamic channel-policy helpers, feature-aware clipboard/channel helper inputs, auth-token parser/verifier, bounded PAM conversation inputs, bounded NTLM proof binding against length-counted ANSI/Unicode delegated identities in every domain mode, explicit IPC wire header/payload decoder boundaries including clipboard messages, strict clipboard UTF-8/UTF-16 conversion roundtrips, agent frame-response/pump-budget policy helpers, agent input event bounds plus stateful surrogate-decoder invariants, display-number/reservation-path policy bounds, and in-memory session state/cleanup/reconnect/resource-limit policy helpers through LibFuzzer; sustained corpora and broader selected RDP input fuzzing remain open.
- `server/frdp/test/e2e/scripts/rdp-protocol-regression.sh` provides a retained-client auth-only protocol regression probe across a small NLA option matrix with per-case logs, `rdp-session-smoke.sh` provides a retained-client graphical disconnect/reconnect lifecycle probe with stable session-identity and cleanup checks, and `TestFreeRDPFrdpE2EScripts` syntax-checks the E2E shell scripts plus expected Compose profiles/services, fixture files, bounded profile/probe timeout knobs, and fail-fast retained-client tool checks including `nc`, `frdpctl`, and XWD capture where those probes require them. These probes have not been promoted to a CI gate and do not cover codecs, channel behavior, reconnect across restart, Windows clients or long-running interop regressions.
- `server/frdp/test/e2e/scripts/rdp-load-probe.sh` provides a retained-client auth-only load probe with configurable concurrency, iterations and timeout validation. `TestFreeRDPFrdpE2EScripts` keeps the harness scripts/fixtures parseable and uses a Docker-free runner fixture to enforce single-run compatibility, repeated fail-fast behavior, and incomplete-attempt preservation, but the load probe has not been promoted to a CI gate and does not cover graphical session load, soak, memory-growth, reconnect or crash-recovery behavior.
- `packaging/monitoring/frdpd-node-exporter-textfile.sh`, `frdpd-prometheus-alerts.yml`, and `frdpd-grafana-dashboard.json` install as starter monitoring examples and cover `frdpctl status` reachability/session-count export, collector freshness, grouped lifecycle-state export, initial Prometheus alerts, and a matching dashboard for the exported metrics. `TestFreeRDPFrdpMonitoring` validates successful and failed textfile scrape output, max-connection argument rejection, label/comment escaping, the expected alert rules including stale collector and stuck cleanup-state detection, and the expected dashboard metric references, but these examples do not replace native daemon metrics, authentication-rate instrumentation, SLO definitions or production threshold validation.
- `docs/rdp-server-pam-sssd-nla/14-support-matrix.md` now records pilot support candidates, prototype areas, unsupported capabilities, and the evidence required before any GA support claim; the runbook points to it instead of listing unverified versions as supported.
- The Docker Compose E2E harness clears each profile's artifact directory before execution, then preserves rendered Compose config, timestamped aggregate and authoritative server-auth logs, per-container logs, per-container inspect JSON, exit status, XWD captures, and component CTest `LastTest.log` when available. Profile-aware teardown removes the selected profile's containers and volumes before and after execution. `FRDP_E2E_PROFILE_TIMEOUT` bounds each attempt; `FRDP_E2E_REPETITIONS` can repeat selected profiles from clean Compose volumes, stop on first failure, and retain each attempt separately. The source snapshot excludes and verifies exclusion of diagnostic artifact trees. Local PAM has passed two consecutive real-client authentication/session/reconnect runs through this mode; Samba AD/SSSD and joined-host FreeIPA/SSSD with HBAC have green single runs. Weekly provider jobs now request two clean repetitions while manual provider dispatches remain single-run by default; successful repeated provider CI history still needs to accumulate.
- Clean Ubuntu 24.04 and Fedora 42 declared-dependency builds produced installable Debian and RPM artifacts with ABI-private in-tree FreeRDP/WinPR libraries. Separate clean target containers passed APT/DNF installation, package verification, dependency-metadata checks, and private linkage for every packaged binary; the workflow now codifies both gates. The Debian gate also validates scoped DEP-5 runtime metadata and retains a pedantic lintian report on success or failure while rejecting error-level findings; complete source copyright review remains open. Successful package CI history, Windows-client and Kerberos-only interoperability, broader IPA topology/ticket-renewal, broader agent input/framebuffer behavior, and production codec/channel evidence remain open.
